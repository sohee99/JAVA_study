package sohee.stream;

import java.util.Arrays;
import java.util.List;


public class ForEach {
    /*
    List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);
    list.stream().filter(n->n%2 ==0).forEach(n -> System.out.println(n));   // [2, 4]
    */
}
