package sohee.stream;

import java.nio.channels.SeekableByteChannel;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.stream.Collectors;

public class Collect3 {
    /*
    public static void main(String[] args) {

        List<Integer> list = Arrays.asList(1,2,3,3,4,5);

        HashSet<Integer> collect;
        collect = list.stream().filter(n->n%2 ==0).collect(Collectors.toCollection(HashSet::new));

        set.stream().forEach(System.out::println);  // {1, 3, 5}//
        }
        */
}
